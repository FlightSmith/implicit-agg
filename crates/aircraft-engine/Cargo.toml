[package]
name = "aircraft-engine"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
aircraft-geom = { version = "0.1.0", path = "../aircraft-geom" }
aircraft-graph = { version = "0.1.0", path = "../aircraft-graph" }
aircraft-model = { version = "0.1.0", path = "../aircraft-model" }
meshio = { version = "0.1.0", path = "../meshio" }
serde_json.workspace = true

[lints]
workspace = true
